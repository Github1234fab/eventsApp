// import { jsonDataByDate } from "../lib/store.js"; // Assurez-vous que ce chemin est correct

// export async function getEventById(id) {
//         let event = null;
//         jsonDataByDate.subscribe((value) => {
//                 Object.keys(value).forEach((date) => {
//                         const foundEvent = value[date].find((event) => event.id === id);
//                         if (foundEvent) {
//                                 event = foundEvent;
//                         }
//                 });
//         });
//         return event; // Assurez-vous que cet événement est valide
// }
