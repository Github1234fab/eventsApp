import { writable } from "svelte/store";

// export const jsonServices = writable({});

export const headingsTab = writable([]);

export const filterCategorie = writable("");
export const filterObject = writable("");
export const filterLieu = writable("");